import { ComingSoon } from "@/components/shared/coming-soon";

export default function AdminUsersPage() {
  return (
    <ComingSoon
      title="User Management"
      description="Search, filter, approve, reject, block, and manage student and trainer accounts."
      phase="Phase 3"
    />
  );
}
