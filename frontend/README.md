# SSR Portal — Frontend

Next.js (App Router) + TypeScript + Tailwind + shadcn/ui frontend for SSR Portal.

See the [root README](../README.md) for architecture, setup instructions, and API reference.

```bash
npm install
npm run dev   # http://localhost:3000
```
